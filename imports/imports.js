const botaoMenu=document.getElementById('menu-mobile');const menu=document.getElementById('menu-principal');const linksMenu=[...document.querySelectorAll('#menu-principal a[href^="#"]')];
if(botaoMenu&&menu){botaoMenu.addEventListener('click',()=>{const aberto=menu.classList.toggle('ativo');botaoMenu.setAttribute('aria-expanded',String(aberto));botaoMenu.setAttribute('aria-label',aberto?'Fechar menu':'Abrir menu');botaoMenu.textContent=aberto?'×':'☰'});document.querySelectorAll('#menu-principal a').forEach(link=>link.addEventListener('click',()=>{menu.classList.remove('ativo');botaoMenu.setAttribute('aria-expanded','false');botaoMenu.setAttribute('aria-label','Abrir menu');botaoMenu.textContent='☰'}));window.addEventListener('resize',()=>{if(window.innerWidth>850){menu.classList.remove('ativo');botaoMenu.setAttribute('aria-expanded','false');botaoMenu.textContent='☰'}})}
const chips=[...document.querySelectorAll('.filter-chip')];const cards=[...document.querySelectorAll('.preview-card')];chips.forEach(chip=>chip.addEventListener('click',()=>{const filtro=chip.dataset.filter;chips.forEach(c=>c.classList.toggle('ativo',c===chip));cards.forEach(card=>{const categorias=(card.dataset.category||'').split(' ');card.classList.toggle('oculto',filtro!=='todos'&&!categorias.includes(filtro))})}));
const detalhes=[...document.querySelectorAll('.faq details')];detalhes.forEach(item=>item.addEventListener('toggle',()=>{if(item.open){detalhes.forEach(outro=>{if(outro!==item)outro.open=false})}}));
const revealEls=[...document.querySelectorAll('.reveal')];if('IntersectionObserver'in window&&!window.matchMedia('(prefers-reduced-motion: reduce)').matches){const obs=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visivel');obs.unobserve(entry.target)}}),{threshold:.12});revealEls.forEach(el=>obs.observe(el))}else{revealEls.forEach(el=>el.classList.add('visivel'))}
const secoes=['conceito','preview','marcas','processo','duvidas'].map(id=>document.getElementById(id)).filter(Boolean);if('IntersectionObserver'in window){const navObs=new IntersectionObserver(entries=>{const visiveis=entries.filter(e=>e.isIntersecting).sort((a,b)=>b.intersectionRatio-a.intersectionRatio);if(!visiveis.length)return;const id=visiveis[0].target.id;linksMenu.forEach(link=>link.classList.toggle('ativo-secao',link.getAttribute('href')===`#${id}`))},{rootMargin:'-30% 0px -55% 0px',threshold:[0,.1,.25,.5]});secoes.forEach(sec=>navObs.observe(sec))}
